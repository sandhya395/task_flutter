import 'package:flutter/material.dart';

class UiDesign4Screen extends StatelessWidget {
  const UiDesign4Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
