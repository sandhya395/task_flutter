import 'package:flutter/material.dart';

 class UiDesignScreen3 extends StatelessWidget {
  const UiDesignScreen3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white24,
    );
  }
}
